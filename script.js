$(document).ready(function() {
  $('.slide').slide({

    // enable autoplay
    start: false,

    // loop through the images at a specified speed
    speed: 3000,

    // horizontal or fade
    animate: "horizontal"
  })
;})